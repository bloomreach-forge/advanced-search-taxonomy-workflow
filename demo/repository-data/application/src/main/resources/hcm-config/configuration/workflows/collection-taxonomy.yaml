definitions:
  config:
    /hippo:configuration/hippo:workflows/collection-taxonomy:
      /collections:
        /frontend:renderer:
          taxonomy.name:
            operation: override
            type: string
            value: Priority
